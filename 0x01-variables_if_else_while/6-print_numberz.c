#include <stdio.h>
/**
 * Description - prints numbers in single digit
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	for (char a = 0; a < 10; a++)
		putchar(a);
	return (0);
}
